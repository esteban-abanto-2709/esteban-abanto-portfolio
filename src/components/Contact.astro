---

---

<section id="contact" class="py-24 px-4">
  <div class="max-w-3xl mx-auto text-center">
    <h2 class="text-4xl font-bold text-white mb-8">Contáctame</h2>
    <p class="text-gray-400 mb-12">
      ¿Tienes un proyecto o colaboración en mente? Envíame un mensaje.
    </p>

    <form
      class="glass-card p-8 rounded-2xl border border-white/10 grid gap-6 text-left"
    >
      <div>
        <label for="name" class="block text-gray-300 mb-2">Nombre</label>
        <input
          type="text"
          id="name"
          name="name"
          required
          class="input-glass w-full"
        />
      </div>

      <div>
        <label for="email" class="block text-gray-300 mb-2"
          >Correo electrónico</label
        >
        <input
          type="email"
          id="email"
          name="email"
          required
          class="input-glass w-full"
        />
      </div>

      <div>
        <label for="message" class="block text-gray-300 mb-2">Mensaje</label>
        <textarea
          id="message"
          name="message"
          rows="5"
          required
          class="input-glass w-full"></textarea>
      </div>

      <button
        type="submit"
        class="premium-button w-fit px-8 py-3 rounded-xl text-white font-semibold text-lg"
      >
        Enviar
      </button>
    </form>
  </div>
</section>
